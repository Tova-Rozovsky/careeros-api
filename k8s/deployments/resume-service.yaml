apiVersion: apps/v1
kind: Deployment
metadata:
  name: resume-service
  namespace: careeros-api
spec:
  replicas: 2 
  selector:
    matchLabels:
      app: resume-service
  template:
    metadata:
      labels:
        app: resume-service
    spec:
      containers:
      - name: resume-service
        image: careeros/resume-service:latest # Replace with actual registry tag
        ports:
        - containerPort: 8080
        envFrom:
        - configMapRef:
            name: careeros-api-config
        env:
        - name: SPRING_DATASOURCE_USERNAME
          valueFrom:
            secretKeyRef:
              name: careeros-api-secrets
              key: DB_USER
        - name: SPRING_DATASOURCE_PASSWORD
          valueFrom:
            secretKeyRef:
              name: careeros-api-secrets
              key: DB_PASSWORD
        readinessProbe:
          httpGet:
            path: /actuator/health/readiness
            port: 8080
          initialDelaySeconds: 20
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /actuator/health/liveness
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 20